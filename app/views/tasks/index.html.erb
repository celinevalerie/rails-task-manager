<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= link_to task.title, task_path(task.id) %>
    <%= link_to "x", delete_path(task.id), method: :delete %>
  </li>
  <% end %>
</ul>

<%= link_to "New Task", "/tasks/new" %>

